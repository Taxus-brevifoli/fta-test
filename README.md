# SmartFTA-Dola

## 快速启动

启动前端，在`./frontend`下执行如下两个指令
> [!WARNING] 
> 注意：如下指令以及前端依赖于Node.js环境，[Node.js下载官网](https://nodejs.org/en/download/archive/v22.22.1)
> 为了方便快捷，不建议使用docker安装nodejs。

``` shell
npm install -g pnpm # 全局安装pnpm
pnpm install        # 安装前端所需要的依赖（非全局）
pnpm run dev        # 启动前端项目
```